const initialState = {
  fontSize: 23,
  lineHeight: 1.4,
  textWidth: 600
}

const typography = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default typography
